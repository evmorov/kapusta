(local target 0)

(fn steps-to-zero [n]
  (var x n)
  (var steps 0)
  (while (not= x target)
    (if (= 0 (% x 2))
        (set x (: (/ x 2) :floor))
        (set x (- x 1)))
    (set steps (+ steps 1)))
  steps)

(print (steps-to-zero 14))
(print (steps-to-zero 8))
(print (steps-to-zero 123))
