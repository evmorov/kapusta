(class Counter)

(fn initialize [start]
  (set (ivar n) start))

(fn tick []
  (set (ivar n) (+ (ivar n) 1)))

(fn value []
  (ivar n))

(fn self.zero []
  (Counter.new 0))

(let [c (Counter.new 10)]
  (c.tick)
  (c.tick)
  (print (c.value)))
