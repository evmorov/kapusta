(fn normalize-word [word]
  (-> word
      (: :downcase)
      (: :chars)
      (: :sort)))

(fn anagram? [a b]
  (= (normalize-word a)
    (normalize-word b)))

(print (anagram? "listen" "silent"))
(print (anagram? "apple" "papel"))
(print (anagram? "hello" "world"))
